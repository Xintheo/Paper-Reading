<%*
// 定义模板选择项
const templateChoices = [
    {
        name: "📝 复制粘贴至其他库",
        file: "0-辅助材料/4-Template/3.笔记整理/笔记整理-1复制粘贴至其他库",
        type: "template"
    },
    {
        name: "📝 属性关联",
        file: "0-辅助材料/4-Template/3.笔记整理/笔记整理-2属性关联",
        type: "template"
    },
    {
        name: "📝 批量复制整理笔记至输出文档",
        file: "0-辅助材料/4-Template/3.笔记整理/笔记整理-4批量复制至输出文档",
        type: "template"
    },
    {
        name: "📝 输出Word",
        file: "0-辅助材料/4-Template/3.笔记整理/笔记整理-3输出Word",
        type: "template"
    }
];

// 显示选择菜单
const choice = await tp.system.suggester(
    templateChoices.map(c => c.name),
    templateChoices
);

if (!choice) {
    // 用户取消选择
    tR = "";
} else if (choice.type === "template") {
    // 加载模板
    try {
        // 方法1：使用 tp.file.include
        const templateContent = await tp.file.include(`[[${choice.file}]]`);
        tR = templateContent;
    } catch (error) {
        try {
            // 方法2：直接读取文件并通过 Templater 处理
            const templateFile = app.vault.getAbstractFileByPath(choice.file + ".md");
            if (templateFile) {
                const content = await app.vault.read(templateFile);
                tR = content;
            } else {
                new Notice(`❌ 找不到模板文件: ${choice.file}.md`);
                tR = "";
            }
        } catch (error2) {
            new Notice(`❌ 加载模板失败: ${error2.message}`);
            console.error("模板加载错误:", error2);
            tR = "";
        }
    }
}
-%>
